namespace Helper.Shared {

    public static class SharedEnums {

        public enum RequestResult {
            Failed,
            Success
        }

        public enum Role {
            Student,
            Teacher
        }
    }
}
